<h1>Add new category</h1>

<%= form_with model: @category, url: categories_path(@user), data: {turbo: false}  do |f| %>
  <% if @category.errors.any? %>
    <div style="color: red">
      <ul>
        <% @category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :icon %>
    <%= f.select :icon, 
        options_for_select(%w[🥣 👩‍👩‍👧‍👦 🛒 📱 📚 🏡 ✈ 👕 🚗]) 
    %>
  </div>
  
  <div>
    <%= f.submit %>
  </div>
<% end %>

<br>

